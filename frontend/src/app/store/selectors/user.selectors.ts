import { createSelector } from '@ngrx/store';
import { User } from '../../models/user';

// export const findUser = createSelector(
//     (stickies: User[], props: { id: number}) => {
//         return stickies.find(user => user.jwtToken === props.id);
//     }
// )